This file should provide tests for all functions in texcheck

Missing space in a new sentence.Missing space in a new sentence

Missing space before a(bracket)

Here is (a)missing space after bracket

Extra space after  a ( bracket)

Extra space before  a (bracket )

Missing capital in new sentence. missing capital in new sentence.

Missing capital in new sentence. different spacing.

These are problematic e.g. this or i.e. that or Dr. and Prof. Whoever


Missing capital in new sentence.
over a line break

Repeated repeated words words on a single line

Repeated words
words across a line break

This is a misplaced footnote. \footnote{wrong}
And so is this.  \footnote{wrong}
And this\footnote{wrong}.
Finally.\footnote{right}

This citing\cite{ref} and \cite{ref} is inconsistent.

This can cause problems for % my script

This can cause problems % for my script

A error

An mistake

Check configuration file \cite{eagbe} \unknown{Abe}
